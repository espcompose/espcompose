{
  "name": "@espcompose/simulator-bridge",
  "version": "0.1.1",
  "private": true,
  "scripts": {
    "test": "python3 -m venv .venv && .venv/bin/pip install --quiet -r src/requirements.txt && .venv/bin/python -m pytest src/ -v"
  }
}
